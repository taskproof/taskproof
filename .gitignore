.env
.env.*
node_modules/
dist/
.turbo/
coverage/
__pycache__/
.venv/
.pytest_cache/
.DS_Store
gauntlet-runs/

# Internal planning & strategy — never published (this repo is destined to be public).
# Kept on disk for local work; git-ignored so they can never enter public history.
# Anything under docs/internal/ is private by convention; add new strategy docs there.
/CLAUDE.md
/docs/internal/
